const user= document.querySelector(".user");

if (user !== null){
    // show
} else {
    // hide & paint
}

//paint
const userName_CN=`userName`;
const userName= document.querySelector(".userName");
function paint(){
    userName.classList(userName_CN);
}